# 広島 電子黒板：Slack連携お知らせ・TODOリストのセットアップ手順（2026-09-24）

電子黒板のホーム画面に **📢お知らせ（Slack連携）** と **✅TODO** を追加しました。
どちらも「設定しないあいだは案内文が出るだけ」で、他の機能には一切影響しません。

---

## 0. まず必須：GASの再デプロイ

`gas/広島連携.gs` の中身を全部コピーして、
「【広島】電子黒板データ」スプレッドシート → **拡張機能 → Apps Script** のエディタに
全部貼り直して保存（💾）したあと、

> **デプロイ → デプロイを管理 → 鉛筆アイコン → バージョン「新バージョン」 → デプロイ**

★「実行」ボタンを押すだけでは公開URL側に反映されません。必ず上の手順まで行ってください。

再デプロイすると、この時点で次の2つはすぐ使えるようになります。

- **TODOリスト**（下の「2.」を読んでシートに業務を書くだけ）
- **表示が速くなる仕組み**（下の「3.」）

---

## 1. 📢 お知らせ（Slack連携・広島専用チャンネル）

Slackの広島用チャンネルに書いた内容が、そのまま電子黒板に出ます（承認ステップなし）。
投稿の**先頭に `【荷造り】`** を付けると、「📋本日荷造り」タブにも短い注意文が出ます。

### 1-1. Slackアプリ（Bot）を用意する

センターの「電子黒板お知らせ」アプリが既にあるので、**同じアプリを使い回せます**
（新しくアプリを作る必要はありません）。

1. https://api.slack.com/apps を開く → 既存の **電子黒板お知らせ** アプリを選ぶ
2. 左メニュー **OAuth & Permissions** → 下の **Bot Token Scopes** に次があるか確認
   （足りなければ追加 → 上部の「Reinstall to Workspace」）

   | スコープ | 用途 |
   |---|---|
   | `channels:history` | 公開チャンネルの投稿を読む |
   | `groups:history` | 非公開チャンネルの投稿を読む |
   | `users:read` | 投稿者の名前を表示する |

3. 同じ画面の **Bot User OAuth Token**（`xoxb-…`）をコピーして控える

> 新しくアプリを作る場合は「Create New App → From scratch」で作り、
> 上の3スコープを付けて「Install to Workspace」してください。

### 1-2. 広島用チャンネルを決めて、Botを招待する

1. Slackで広島用のチャンネルを開く（無ければ新規作成。例：`広島-お知らせ`）
2. メッセージ欄に `/invite @電子黒板お知らせ` と打ってBotを招待する
   （★これをしないと読めません）
3. チャンネル名をクリック → 一番下の **チャンネルID（`C…`）** をコピーして控える

### 1-3. GASに登録する

Apps Scriptエディタ左の **⚙ プロジェクトの設定 → スクリプト プロパティ → プロパティを追加**
で、次の2つを登録して保存します。

| プロパティ名 | 値 |
|---|---|
| `SLACK_BOT_TOKEN` | 1-1でコピーした `xoxb-…` |
| `SLACK_CHANNEL_ID` | 1-2でコピーした `C…` |

> トークンはHTML（GitHubで公開されているファイル）には一切書きません。
> ここに入れておけば外から見えません。

### 1-4. 動作確認

エディタ上部の関数選択で **`testNews`** を選んで **▶実行**
（初回は権限の承認ダイアログが出るので「許可」）。

実行ログに投稿の一覧が出れば成功です。エラーが出たときは：

| ログに出る文字 | 原因 |
|---|---|
| `SLACK_BOT_TOKEN / SLACK_CHANNEL_ID をスクリプトプロパティに設定してください` | 1-3 の登録漏れ |
| `not_in_channel` | 1-2 の `/invite` 漏れ |
| `missing_scope` | 1-1 のスコープ不足（追加後 Reinstall が必要） |

電子黒板を開き直すと、1分以内にお知らせが表示されます。

### 1-5. 運用イメージ

1. 広島用チャンネルに連絡を書く
2. 1分以内に電子黒板の「📢お知らせ」に「〇〇さんからのお知らせ」として出る（当日は **NEW**）
3. 荷造りに関わる注意は **先頭に `【荷造り】`** を付ける
   → 「📋本日荷造り」タブの上にも注意文が出る（クリックで全文）
4. カードの「🔍過去のお知らせを見る」で直近14日分を一覧表示

> ⚠ このチャンネルに書いた内容は**全部そのまま黒板に出ます**。
> 黒板に出したくない連絡は別のチャンネルで行ってください。

---

## 2. ✅ TODOリスト

「【広島】電子黒板データ」スプレッドシートに **「広島TODOマスタ」** シートが
自動で作られます（再デプロイ後、電子黒板を一度開けば作られます）。

このシートに行を足すだけで黒板に出ます。**GASの再デプロイは不要**です。

| A列（業務） | B列（頻度） |
|---|---|
| 包丁研ぎチェック | 毎日 |
| フィルターの清掃 | 週1 |
| 計量器の点検 | 月1 |

- 1行目は見出し（`業務` / `頻度`）です。2行目から書いてください。
- 頻度（B列）は見出しラベルとして出るだけなので、空欄でも構いません。
- 黒板でチェックを入れる／外すと、**「TODO履歴」シート**に1行ずつ記録されます
  （上書きしないので、誰がいつ押したかが残ります）。
- チェック状態は日付ごとです。翌日には自動的に全部未チェックに戻ります。
- 複数のPCで見ていても、30秒以内に他のPCのチェックが反映されます。

---

## 3. ⚡ 表示を速くする仕組み（「開くのが遅い」対策）

今回、次の3段構えで高速化しました。**1・2は再デプロイだけで有効になります。**

1. **GASの中の無駄な読み直しをやめた**
   これまで1回の`bundle`で発注書スプレッドシートを5回も開き直していたのを、1回だけにしました。
2. **組み立てた結果を一時保存（キャッシュ）するようにした**
   2回目以降は保存済みの結果をそのまま返すので、待ち時間がほぼ通信時間だけになります。
3. **電子黒板が「前回の内容」をすぐ表示するようにした**
   画面を開いた瞬間に前回の数字が出て、そのあと最新に差し替わります（体感ゼロ秒）。

### 3-1.（任意・おすすめ）キャッシュを常に温めておく

朝いちなど、しばらく誰も開いていなかった時だけは作り直しの待ち時間が発生します。
これも無くしたい場合は、エディタで **`installBoardCacheTrigger`** を **1回だけ▶実行**してください。

- 5分おきに裏側でデータを用意しておくトリガーが設置されます
- **5時〜19時のあいだだけ**動きます（Apps Scriptの1日あたりの実行時間上限を使い切らないため）
- やめたいときは **`uninstallBoardCacheTrigger`** を▶実行

### 3-2. 効果を測る

エディタで **`testBundleSpeed`** を▶実行すると、
「組み立て（キャッシュ無し）」と「キャッシュ経由」の秒数がログに出ます。

---

## 4. 今回入れなかったもの

- **ヘルプ要請（Slackへ送信）ボタン** … 広島では運用が固まっていないため、今回は見送り
  （センター電子黒板にはあります）
- **Googleカレンダー連携のTODO** … 広島にカレンダー運用が無いため未実装
  （センターは「センター◯◯」という予定をTODOに取り込んでいます）
